# GUI_HM_fit

Repositorio de prototipos para HM Fit.

## GUI wxPython (experimental)

Versión 100% Python (sin Tauri/Node) que ejecuta los procesadores directamente y muestra gráficas estáticas (PNG) en la GUI.

- Código: `hmfit_wx/`
- Ejecutar: `python -m hmfit_wx`

Dependencias:
- `pip install -r backend_fastapi/requirements.txt`
- `pip install -r requirements_wx.txt`
