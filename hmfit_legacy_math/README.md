# hmfit_legacy_math

This folder contains archived legacy math/UI code kept only for reference.

- Do **not** import or call this code from the wxPython GUI.
- All calculations must go through `hmfit_core` which calls `backend_fastapi` processors as a library.

